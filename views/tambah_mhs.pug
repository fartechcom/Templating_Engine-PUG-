<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title #{title}
    body
        a(href="/") Daftar Mahasiswa | 
        a(href="/tambah_mhs") Tambah Mahasiswa
        hr
        h1 Tambah Mahasiswa

        form(action="/tambah_mhs", method="post")
            label(for="nama") Nama
            input(type="text", name="nama")#nama

            br
            br
            input(type="submit", value="Kirim")